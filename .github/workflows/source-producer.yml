name: Source Producer

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: source-producer-${{ github.ref }}
  cancel-in-progress: false

env:
  GERBIL_REF: 07c8481588a8b07dbf05832687817cd398902ac0
  GERBIL_VERSION_LABEL: v0.18.2-07c84815

jobs:
  linux-capability:
    name: Linux capability (${{ vars.GERBIL_SOURCE_RUNNER || 'ubuntu-latest' }})
    runs-on: ${{ vars.GERBIL_SOURCE_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    steps:
      - name: Check out gerbil-bazel
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Linux producer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            build-essential \
            ca-certificates \
            ccache \
            coreutils \
            git \
            jq \
            libsqlite3-dev \
            libssl-dev \
            openssl \
            pkg-config \
            sqlite3 \
            zlib1g-dev

      - name: Validate source build identity semantics
        run: |
          tools/ci/test_source_build_identity.sh
          tools/ci/test_source_build_checkpoint.sh
          tools/ci/test_bootstrap_gerbil.sh
          tools/ci/test_gerbil_bootstrap_attempt.sh
          tools/ci/test_install_materialization.sh
          tools/ci/test_source_producer_workflow.sh

      - name: Resolve Linux source build identity
        id: gerbil-source-build-identity
        env:
          GERBIL_REF: ${{ env.GERBIL_REF }}
          GERBIL_SOURCE_BUILD_IDENTITY_RECEIPT: ${{ github.workspace }}/.ci/gerbil-source-build-identity.json
        run: tools/ci/source_build_identity.sh >> "$GITHUB_OUTPUT"

      - name: Restore Linux compiler cache
        id: gerbil-compiler-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .ci/ccache
          key: gerbil-ccache-${{ runner.os }}-${{ runner.arch }}-${{ steps.gerbil-source-build-identity.outputs.compiler_cache_namespace_digest }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            gerbil-ccache-${{ runner.os }}-${{ runner.arch }}-${{ steps.gerbil-source-build-identity.outputs.compiler_cache_namespace_digest }}-
            gerbil-ccache-${{ runner.os }}-${{ runner.arch }}-${{ env.GERBIL_VERSION_LABEL }}-

      - name: Restore Linux Gerbil installation
        id: gerbil-install-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .ci/gerbil-prefix
          key: gerbil-install-v1-${{ steps.gerbil-source-build-identity.outputs.install_digest }}

      - name: Restore Linux source-build checkpoint
        id: gerbil-source-build-checkpoint
        if: steps.gerbil-install-cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .ci/gerbil-source-build-checkpoint
          key: gerbil-source-build-checkpoint-v1-${{ steps.gerbil-source-build-identity.outputs.install_digest }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            gerbil-source-build-checkpoint-v1-${{ steps.gerbil-source-build-identity.outputs.install_digest }}-

      - name: Admit source producer execution
        env:
          GERBIL_INSTALL_CACHE_HIT: ${{ steps.gerbil-install-cache.outputs.cache-hit }}
          GERBIL_CONFIGURED_RUNNER: ${{ vars.GERBIL_SOURCE_RUNNER }}
          GERBIL_SELECTED_RUNNER: ${{ vars.GERBIL_SOURCE_RUNNER || 'ubuntu-latest' }}
        run: |
          tools/ci/source_producer_admission.sh \
            "$GERBIL_INSTALL_CACHE_HIT" \
            "$GERBIL_CONFIGURED_RUNNER" \
            "$GERBIL_SELECTED_RUNNER" \
            .ci/receipts/source-producer-admission.json

      - name: Build Gerbil on Linux
        id: gerbil-build
        if: steps.gerbil-install-cache.outputs.cache-hit != 'true'
        env:
          CCACHE_DIR: ${{ github.workspace }}/.ci/ccache
          CCACHE_MAXSIZE: 2G
          GERBIL_BOOTSTRAP_ATTEMPT_RECEIPT: ${{ github.workspace }}/.ci/receipts/gerbil-bootstrap-attempt.json
          GERBIL_BUILD_TIMEOUT_MINUTES: ${{ steps.gerbil-source-build-identity.outputs.build_timeout_minutes }}
          GERBIL_PREFIX: ${{ github.workspace }}/.ci/gerbil-prefix
          GERBIL_REQUIRE_CCACHE: "1"
          GERBIL_SOURCE_BUILD_CONFIG: ${{ github.workspace }}/tools/ci/gerbil_source_build_config.json
          GERBIL_SOURCE_BUILD_CHECKPOINT_ROOT: ${{ github.workspace }}/.ci/gerbil-source-build-checkpoint
          GERBIL_SOURCE_BUILD_IDENTITY_RECEIPT: ${{ github.workspace }}/.ci/gerbil-source-build-identity.json
          GERBIL_SRC: ${{ github.workspace }}/.ci/gerbil-src
          GERBIL_TERMINATION_GRACE_SECONDS: ${{ steps.gerbil-source-build-identity.outputs.termination_grace_seconds }}
        run: |
          set +e
          tools/ci/run_gerbil_bootstrap_attempt.sh \
            "$GERBIL_BUILD_TIMEOUT_MINUTES" \
            "$GERBIL_TERMINATION_GRACE_SECONDS" \
            "$GERBIL_SOURCE_BUILD_IDENTITY_RECEIPT" \
            "$GERBIL_BOOTSTRAP_ATTEMPT_RECEIPT" \
            tools/ci/bootstrap_gerbil.sh
          build_status=$?
          set -e
          checkpoint_available=false
          if tools/ci/source_build_checkpoint.sh \
            validate \
            "$GERBIL_SOURCE_BUILD_CHECKPOINT_ROOT" \
            "$GERBIL_SRC" \
            "$GERBIL_PREFIX" \
            "$GERBIL_SOURCE_BUILD_IDENTITY_RECEIPT"; then
            checkpoint_available=true
          fi
          echo "checkpoint_available=$checkpoint_available" >> "$GITHUB_OUTPUT"
          exit "$build_status"

      - name: Save Linux compiler cache
        if: >-
          always() &&
          steps.gerbil-install-cache.outputs.cache-hit != 'true' &&
          (steps.gerbil-build.outcome == 'success' ||
          steps.gerbil-build.outcome == 'failure')
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .ci/ccache
          key: ${{ steps.gerbil-compiler-cache.outputs.cache-primary-key }}

      - name: Save Linux source-build checkpoint
        if: >-
          always() &&
          steps.gerbil-install-cache.outputs.cache-hit != 'true' &&
          steps.gerbil-build.outcome == 'failure' &&
          steps.gerbil-build.outputs.checkpoint_available == 'true'
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .ci/gerbil-source-build-checkpoint
          key: ${{ steps.gerbil-source-build-checkpoint.outputs.cache-primary-key }}

      - name: Record Linux Gerbil materialization
        env:
          GERBIL_INSTALL_CACHE_HIT: ${{ steps.gerbil-install-cache.outputs.cache-hit == 'true' }}
          GERBIL_INSTALL_DIGEST: ${{ steps.gerbil-source-build-identity.outputs.install_digest }}
          GERBIL_INSTALL_MATERIALIZATION_RECEIPT: ${{ github.workspace }}/.ci/receipts/gerbil-install-materialization-${{ runner.os }}-${{ runner.arch }}.json
          GERBIL_PREFIX: ${{ github.workspace }}/.ci/gerbil-prefix
          GERBIL_RUNNER_NAME: ${{ runner.name }}
        run: tools/ci/record_install_materialization.sh

      - name: Activate the produced Gerbil capability
        run: |
          echo "$GITHUB_WORKSPACE/.ci/gerbil-prefix/bin" >> "$GITHUB_PATH"
          echo "GERBIL_PREFIX=$GITHUB_WORKSPACE/.ci/gerbil-prefix" >> "$GITHUB_ENV"

      - name: Verify the produced Gerbil capability
        run: |
          command -v gxi
          command -v gxc
          gxi --version

      - name: Set up Bazel
        uses: bazel-contrib/setup-bazel@c5acdfb288317d0b5c0bbd7a396a3dc868bb0f86 # 0.19.0
        with:
          bazelisk-cache: true
          repository-cache: true
          disk-cache: gerbil-source-producer-${{ runner.os }}-${{ runner.arch }}-${{ env.GERBIL_VERSION_LABEL }}

      - name: Package the relocatable Linux capability
        env:
          GERBIL_CAPABILITY_ARCHIVE: ${{ github.workspace }}/.ci/capabilities/gerbil-${{ env.GERBIL_VERSION_LABEL }}-linux-x86_64-${{ steps.gerbil-source-build-identity.outputs.install_digest }}.tar.gz
          GERBIL_SOURCE_BUILD_IDENTITY_RECEIPT: ${{ github.workspace }}/.ci/gerbil-source-build-identity.json
        run: tools/ci/package_prebuilt_capability.sh

      - name: Validate the packaged Linux capability as a consumer
        env:
          GERBIL_PREBUILT_ARCHIVE: ${{ github.workspace }}/.ci/capabilities/gerbil-${{ env.GERBIL_VERSION_LABEL }}-linux-x86_64-${{ steps.gerbil-source-build-identity.outputs.install_digest }}.tar.gz
        run: tools/ci/test_repository_provider.sh prebuilt

      - name: Upload the Linux capability
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: gerbil-prebuilt-${{ env.GERBIL_VERSION_LABEL }}-linux-x86_64-${{ steps.gerbil-source-build-identity.outputs.install_digest }}
          path: .ci/capabilities/*
          if-no-files-found: error

      - name: Upload producer receipts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: gerbil-source-producer-${{ runner.os }}-${{ runner.arch }}-receipt
          path: |
            .ci/gerbil-source-build-identity.json
            .ci/receipts/*.json
            .ci/gerbil-prefix/bootstrap.receipt.json
            .ci/gerbil-source-build-checkpoint/current.json
            .ci/gerbil-source-build-checkpoint/generations/*/receipt.json
          if-no-files-found: warn
